import java.util.*;
public class CircleClient
{
    public static void main(String[]args)
    {
        Circle newish=new Circle();
        Scanner scan=new Scanner(System.in);
        System.out.println("Please enter an integer:");
        int i=scan.nextInt();
        newish.setRadius(i);
        newish.toString();
        newish.equals(Airport.class);
    }
}
